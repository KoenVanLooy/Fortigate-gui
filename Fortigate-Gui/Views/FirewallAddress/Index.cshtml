@model IEnumerable<Fortigate_Gui.Models.FirewallAddress>

@{
    ViewData["Title"] = "FirewallAddress";
}

<div class="row">
    <div class="col-4">
        <p>
            <a onclick="showInPopup('@Url.Action("Create","FirewallAddress",null,Context.Request.Scheme)','Add FirewallAddress')" class="btn btn-outline-danger">New Firewall Address</a>
        </p>        
    </div>
    <div class="col-8">
        <h1><i class="fas fa-at"></i> Firewall Adresses</h1>
    </div>
</div>
<div id="view-all">
    @await Html.PartialAsync("_ViewAll", Model)
</div>
